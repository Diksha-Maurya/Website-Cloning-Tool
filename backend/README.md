# Backend for Orchids SWE Intern Challenge

This directory contains the FastAPI backend for the website cloning tool.

**To run:**
Use `uv run app.main:app --reload --port 8000` from within this directory.